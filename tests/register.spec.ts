import { test, expect } from '@playwright/test';

test('User can create an account', async ({ page }) => {
    await page.goto('/');
  
  
  });
  
  test('User is given an error message for invalid email address', async ({ page }) => {
    await page.goto('/');
  
  
  });
  
  test('User is given an error message for invalid password', async ({ page }) => {
    await page.goto('/');
  
  
  });
  
  test('User is given visual queue strength of password', async ({ page }) => {
    await page.goto('/');
  
  
  });

  test('User is given an error message for email already in use', async ({ page }) => {
    await page.goto('/');
  
  
  });